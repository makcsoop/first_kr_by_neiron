# Задание: Обработка данных - Анализатор оценок, уникальные элементы, фильтр товаров

## 📋 Общее описание
Реализуйте три функции для работы с массивами: анализ оценок, поиск уникальных элементов и фильтрацию товаров по цене.

---

## 🔧 Функции для реализации

### Задача 1: `analyzeGrades` - Анализатор оценок

```php
function analyzeGrades(array $grades): array
```

**Параметры:**
- `array $grades` - массив оценок (целые числа от 1 до 5)

**Возвращает:**
- `array` - ассоциативный массив со статистикой:
  - `average` - средний балл
  - `max` - максимальная оценка
  - `min` - минимальная оценка
  - `count_excellent` - количество отличных оценок (5)
  - `count_unsatisfactory` - количество неудовлетворительных оценок (1 и 2)

**Пример:**
```php
// Входные данные
$grades = [5, 4, 3, 5, 2, 1, 4, 5, 3];

// Результат
[
    'average' => 3.56,
    'max' => 5,
    'min' => 1,
    'count_excellent' => 3,
    'count_unsatisfactory' => 2
]
```

---

### Задача 2: `findUniqueElements` - Поиск уникальных элементов

```php
function findUniqueElements(array $numbers): array
```

**Параметры:**
- `array $numbers` - массив чисел с возможными повторениями

**Возвращает:**
- `array` - массив уникальных элементов в порядке первого вхождения

**Требование:**
- ❌ Не использовать `array_unique()`
- ❌ Не использовать другие встроенные функции для работы с уникальными значениями

**Пример:**
```php
// Входные данные
$numbers = [1, 2, 3, 2, 4, 1, 5, 3, 6];

// Результат: [1, 2, 3, 4, 5, 6]
```

---

### Задача 3: `filterProductsByPrice` - Фильтр товаров по цене

```php
function filterProductsByPrice(array $products, float $minPrice, float $maxPrice): array
```

**Параметры:**
- `array $products` - массив товаров (каждый элемент имеет ключи `name` и `price`)
- `float $minPrice` - минимальная цена
- `float $maxPrice` - максимальная цена

**Возвращает:**
- `array` - отфильтрованный массив товаров

**Пример:**
```php
// Входные данные
$products = [
    ['name' => 'Товар 1', 'price' => 100],
    ['name' => 'Товар 2', 'price' => 250],
    ['name' => 'Товар 3', 'price' => 50],
    ['name' => 'Товар 4', 'price' => 300]
];
$minPrice = 100;
$maxPrice = 250;

// Результат
[
    ['name' => 'Товар 1', 'price' => 100],
    ['name' => 'Товар 2', 'price' => 250]
]
```

---

## 🧪 Тестирующая система

### Запуск тестов
```bash
php test_system.php
```

### Структура тестов
```php
$testSystem = new TestSystem();
$results = $testSystem->runAllTests();
$testSystem->printResults($results);
```

**Тестируются:**
- ✅ Корректность расчета статистики оценок
- ✅ Правильность поиска уникальных элементов
- ✅ Корректность фильтрации товаров по цене

---

## 📁 Структура файлов

```
project/
├── solution.php          # Ваше решение (нужно реализовать)
├── test_system.php       # Тестирующая система
└── README.md            # Это описание
```

---

## 🚀 Инструкция по выполнению

1. **Переключись на своб ветку(Фамилия)**
2. **Реализуйте все три функции**
3. **Протестируйте решение:**
   ```bash
   php test_system.php
   ```
4. **По готовности запуште решение**

---

## ✅ Критерии оценки

| Количество пройденных тестов | Оценка |
|------------------------------|--------|
| 3/3                          | ✅ Отлично |
| 2/3                          | ⚠️ Хорошо |
| 1/3                          | ❌ Удовлетворительно |
| 0/3                          | 💥 Неудовлетворительно |

---

## 💡 Рекомендации по реализации

### Для `analyzeGrades`:
- Используйте цикл для обхода массива
- Считайте сумму, максимум, минимум
- Используйте условные операторы для подсчета отличных и неудовлетворительных оценок

### Для `findUniqueElements`:
- Создайте временный массив для отслеживания уже встреченных элементов
- Используйте `in_array()` для проверки наличия элемента
- Сохраняйте порядок первого вхождения

### Для `filterProductsByPrice`:
- Проверяйте наличие ключа `price` в каждом товаре
- Используйте условие для проверки диапазона цен
- Возвращайте только подходящие товары

---

## 🧪 Примеры тестовых данных

### Тест 1 - Оценки:
```php
$grades = [5, 4, 3, 5, 2, 1, 4, 5, 3];
// Ожидаемый результат: 
// average ≈ 3.56, max = 5, min = 1, excellent = 3, unsatisfactory = 2
```

### Тест 2 - Уникальные элементы:
```php
$numbers = [1, 2, 3, 2, 4, 1, 5, 3, 6];
// Ожидаемый результат: [1, 2, 3, 4, 5, 6]
```

### Тест 3 - Фильтрация товаров:
```php
$products = [
    ['name' => 'A', 'price' => 100],
    ['name' => 'B', 'price' => 250],
    ['name' => 'C', 'price' => 50],
    ['name' => 'D', 'price' => 300]
];
// При min=100, max=250 ожидаем: [A, B]
```

---

## ⚠️ Важные требования

- ❌ Запрещено использовать `array_unique()` во второй задаче
- ✅ Обязательная обработка пустых массивов
- ✅ Проверка наличия требуемых ключей в массивах
- ✅ Корректная обработка граничных значений

---

## 📝 Проверка решения

Решение считается успешным, если:
1. Все тесты проходят без ошибок
2. Функции возвращают ожидаемые результаты
3. Код соответствует требованиям задания
4. Отсутствуют предупреждения и ошибки

---

Удачи в выполнении задания! 🍀